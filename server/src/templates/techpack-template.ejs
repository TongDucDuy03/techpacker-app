<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= pageTitle %></title>
  <style>
    @page {
      size: A4;
      margin: 15mm 20mm;
    }

    body {
      font-family: 'Roboto', Arial, sans-serif;
      font-size: 10px;
      line-height: 1.45;
      color: #1f2933;
      -webkit-print-color-adjust: exact;
      print-color-adjust: exact;
    }

    h1, h2, h3, h4 {
      font-weight: 600;
      color: #111827;
    }

    .page {
      page-break-after: always;
    }

    .page:last-child {
      page-break-after: auto;
    }

    .section-title {
      font-size: 13px;
      text-transform: uppercase;
      letter-spacing: 0.6px;
      border-bottom: 2px solid #111827;
      padding-bottom: 6px;
      margin: 24px 0 12px;
      page-break-after: avoid;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 18px;
      background: #ffffff;
      font-size: 9px;
    }

    thead {
      display: table-header-group;
    }

    tbody {
      display: table-row-group;
    }

    th, td {
      border: 1px solid #cbd5e1;
      padding: 8px 6px;
      vertical-align: top;
      word-break: break-word;
    }

    th {
      background: #f1f5f9;
      text-transform: uppercase;
      font-size: 8px;
      letter-spacing: 0.4px;
      color: #0f172a;
    }

    tr:nth-child(even) td {
      background: #f9fafb;
    }

    .text-right { text-align: right; }
    .text-center { text-align: center; }
    .text-muted { color: #6b7280; }
    .avoid-break { page-break-inside: avoid; break-inside: avoid; }
    .page-break { page-break-before: always; }

    .thumb {
      width: 60px;
      height: 60px;
      object-fit: cover;
      border: 1px solid #d1d5db;
      border-radius: 4px;
      background: #fff;
      display: block;
    }

    .grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 16px;
    }

    .grid-item {
      border: 1px solid #d1d5db;
      border-radius: 6px;
      padding: 10px;
      background: #fff;
      min-height: 120px;
    }

    .grid-item img {
      width: 100%;
      height: 120px;
      object-fit: cover;
      border: 1px solid #cbd5e1;
      border-radius: 4px;
      margin-bottom: 8px;
    }

    .measurement-table th.size-col,
    .measurement-table td.size-col {
      text-align: center;
      font-weight: 600;
    }

    .measurement-table td.tol-minus {
      color: #b91c1c;
      text-align: center;
      font-weight: 600;
    }

    .measurement-table td.tol-plus {
      color: #15803d;
      text-align: center;
      font-weight: 600;
    }

    .group-row td {
      background: #e2e8f0 !important;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.4px;
      color: #0f172a;
    }

    .howto {
      display: flex;
      gap: 16px;
      padding: 14px;
      border: 1px solid #d1d5db;
      border-radius: 6px;
      background: #f8fafc;
      margin-bottom: 18px;
    }

    .howto img {
      width: 320px;
      max-width: 45%;
      object-fit: contain;
      border: 1px solid #cbd5e1;
      border-radius: 4px;
      background: #fff;
    }

    .howto-content h3 {
      font-size: 11px;
      margin-bottom: 8px;
      text-transform: uppercase;
    }

    .notes-list {
      list-style: disc;
      margin-left: 18px;
      line-height: 1.5;
    }

    .care-grid {
      display: grid;
      grid-template-columns: repeat(6, 1fr);
      gap: 12px;
    }

    .care-item {
      text-align: center;
      padding: 10px;
      border: 1px solid #d1d5db;
      border-radius: 6px;
      background: #fff;
    }

    .care-item img {
      width: 48px;
      height: 48px;
      object-fit: contain;
      margin-bottom: 6px;
    }

    .summary-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 12px;
      margin-top: 16px;
    }

    .summary-card {
      border: 1px solid #d1d5db;
      border-radius: 6px;
      padding: 12px;
      background: #f8fafc;
    }

    .summary-card h4 {
      font-size: 10px;
      text-transform: uppercase;
      margin-bottom: 6px;
    }

    .summary-card p {
      margin-bottom: 4px;
      font-size: 9px;
    }

    .meta-table th {
      width: 28%;
    }

    @media print {
      .page {
        page-break-after: always;
      }

      .avoid-break {
        break-inside: avoid;
      }
    }
  </style>
</head>
<body>
  <section class="page cover">
    <%- await include('partials/cover', { meta, summary, printedBy, generatedAt, images }) %>
  </section>

  <% if (bom && bom.rows && bom.rows.length) { %>
    <section class="page">
      <%- await include('partials/bom-table', { bom, strings }) %>
    </section>
  <% } %>

  <% if (bomImages && bomImages.length) { %>
    <section class="page">
      <%- await include('partials/bom-images', { bomImages, strings }) %>
    </section>
  <% } %>

  <% if (measurements && measurements.rows && measurements.rows.length) { %>
    <section class="page">
      <%- await include('partials/measurement-table', { measurements, strings }) %>
    </section>
  <% } %>

  <% if (howToMeasure && howToMeasure.length) { %>
    <section class="page">
      <%- await include('partials/how-to-measure', { howToMeasure, strings }) %>
    </section>
  <% } %>

  <% if (notes && notes.length) { %>
    <section class="page">
      <%- await include('partials/notes', { notes, strings }) %>
    </section>
  <% } %>

  <% if (careSymbols && careSymbols.length) { %>
    <section class="page">
      <%- await include('partials/care-symbols', { careSymbols, strings }) %>
    </section>
  <% } %>
</body>
</html>
