<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>BOM Table</title>
  <style>
    @page {
      size: A4;
      margin: 15mm 20mm;
    }

    body {
      font-family: 'Roboto', 'Arial', 'Helvetica', 'DejaVu Sans', sans-serif;
      font-size: 10px;
      line-height: 1.45;
      color: #1f2933;
      -webkit-print-color-adjust: exact;
      print-color-adjust: exact;
      margin: 0;
      padding: 0;
    }

    .section-title {
      font-size: 13px;
      text-transform: uppercase;
      letter-spacing: 0.6px;
      border-bottom: 2px solid #111827;
      padding-bottom: 6px;
      margin: 0 0 12px 0;
      page-break-after: avoid;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 18px;
      background: #ffffff;
      font-size: 9px;
    }

    thead {
      display: table-header-group;
    }

    th, td {
      border: 1px solid #cbd5e1;
      padding: 8px 6px;
      vertical-align: top;
      word-break: break-word;
    }

    th {
      background: #f1f5f9;
      text-transform: uppercase;
      font-size: 8px;
      letter-spacing: 0.4px;
      color: #0f172a;
    }

    tr:nth-child(even) td {
      background: #f9fafb;
    }

    .text-right { text-align: right; }
    .text-center { text-align: center; }
    .text-muted { color: #6b7280; }
    .avoid-break { page-break-inside: avoid; break-inside: avoid; }

    .color-swatch {
      display: inline-block;
      width: 16px;
      height: 16px;
      border: 1px solid #cbd5e1;
      border-radius: 3px;
      vertical-align: middle;
      flex-shrink: 0;
    }

    .thumb {
      width: 60px;
      height: 60px;
      object-fit: cover;
      border: 1px solid #d1d5db;
      border-radius: 4px;
      background: #fff;
      display: block;
    }

    .summary-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 12px;
      margin-top: 16px;
    }

    .summary-card {
      border: 1px solid #d1d5db;
      border-radius: 6px;
      padding: 12px;
      background: #f8fafc;
    }

    .summary-card h4 {
      font-size: 10px;
      text-transform: uppercase;
      margin-bottom: 6px;
    }

    .summary-card p {
      margin-bottom: 4px;
      font-size: 9px;
    }
  </style>
</head>
<body>
  <div class="section-title"><%= strings.bomTitle %></div>

  <% if (bom && bom.rows && bom.rows.length) { %>
    <table class="bom-table">
    <thead>
      <tr>
        <th style="width:5%;">#</th>
        <th style="width:16%;">ITEM DETAILS</th>
        <th style="width:14%;">Details</th>
        <th style="width:11%;">PLACEMENT</th>
        <th style="width:7%;" class="text-center">QTY</th>
        <th style="width:7%;" class="text-center">UOM</th>
        <th style="width:12%;">COLOR INFO</th>
        <th style="width:12%;">SUPPLIER</th>
        <th style="width:16%;">NOTES</th>
      </tr>
    </thead>
      <tbody>
        <% bom.rows.forEach((row, idx) => { %>
          <tr class="<%= row.thumbnail ? 'avoid-break' : '' %>">
            <td class="text-center"><%= row.lineNumber || idx + 1 %></td>
            <td>
              <% if (row.thumbnail) { %>
                <img src="<%= row.thumbnail %>" alt="<%= row.materialName %>" class="thumb" />
              <% } %>
              <div style="margin-top:6px;">
                <strong><%= row.materialName %></strong>
                <% if (row.materialCode) { %>
                  <div class="text-muted">Code: <%= row.materialCode %></div>
                <% } %>
              </div>
            </td>
            <td>
              <div><strong>Part:</strong> <%= row.part || '—' %></div>
              <% if (row.category) { %>
                <div><strong>Category:</strong> <%= row.category %></div>
              <% } %>
              <% if (row.size) { %>
                <div><strong>Size:</strong> <%= row.size %></div>
              <% } %>
              <% if (row.materialComposition) { %>
                <div style="font-size: 8px; margin-top: 4px;">
                  <strong>Composition:</strong> <%= row.materialComposition %>
                </div>
              <% } %>
              <% if (row.weight) { %>
                <div style="font-size: 8px;"><strong>Weight:</strong> <%= row.weight %></div>
              <% } %>
              <% if (row.width) { %>
                <div style="font-size: 8px;"><strong>Width:</strong> <%= row.width %></div>
              <% } %>
              <% if (row.shrinkage) { %>
                <div style="font-size: 8px;"><strong>Shrinkage:</strong> <%= row.shrinkage %></div>
              <% } %>
              <% if (row.unitPrice) { %>
                <div style="font-size: 8px; margin-top: 4px;"><strong>Unit Price:</strong> <%= row.unitPrice %></div>
              <% } %>
            </td>
            <td><%= row.placement || '—' %></td>
            <td class="text-center"><%= row.quantity || '—' %></td>
            <td class="text-center"><%= row.uom || '—' %></td>
            <td>
              <% if (row.hexCode) { %>
                <div style="display: flex; align-items: center; gap: 4px; margin-bottom: 4px;">
                  <span class="color-swatch" style="background-color: <%= row.hexCode %>; width: 16px; height: 16px; border: 1px solid #cbd5e1; border-radius: 3px; display: inline-block; flex-shrink: 0;"></span>
                  <span style="font-size: 8px; font-weight: 600;"><%= row.hexCode %></span>
                </div>
              <% } %>
              <% if (row.color || row.colorCode) { %>
                <div class="text-muted" style="font-size: 8px;">
                  <strong>Color:</strong> <%= row.colorCode || row.color || '—' %>
                </div>
              <% } %>
              <% if (row.pantone) { %>
                <div class="text-muted" style="font-size: 8px;">
                  <strong>Pantone:</strong> <%= row.pantone %>
                </div>
              <% } %>
              <% if (row.rgbCode) { %>
                <div class="text-muted" style="font-size: 8px;">
                  <strong>RGB:</strong> <%= row.rgbCode %>
                </div>
              <% } %>
              <% if (!row.hexCode && !row.color && !row.colorCode && !row.pantone && !row.rgbCode) { %>
                <div class="text-muted" style="font-size: 8px;">—</div>
              <% } %>
            </td>
            <td>
              <div><%= row.supplier || '—' %></div>
              <% if (row.supplierCode) { %>
                <div class="text-muted" style="font-size: 8px;">Code: <%= row.supplierCode %></div>
              <% } %>
              <% if (row.leadTime) { %>
                <div class="text-muted" style="font-size: 8px;">Lead: <%= row.leadTime %> days</div>
              <% } %>
              <% if (row.moq) { %>
                <div class="text-muted" style="font-size: 8px;">MOQ: <%= row.moq %></div>
              <% } %>
              <% if (row.statusBadge) { %>
                <div style="margin-top:4px;">
                  <span style="background:<%= row.statusBadge.background %>; color:<%= row.statusBadge.color %>; padding:2px 6px; border-radius:4px; font-size:8px; font-weight:600;">
                    <%= row.statusBadge.label %>
                  </span>
                </div>
              <% } %>
            </td>
            <td>
              <div><%= row.comments || '—' %></div>
              <% if (row.careInstructions) { %>
                <div class="text-muted" style="font-size: 8px; margin-top: 4px;">
                  <strong>Care:</strong> <%= row.careInstructions %>
                </div>
              <% } %>
              <% if (row.testingRequirements) { %>
                <div class="text-muted" style="font-size: 8px; margin-top: 4px;">
                  <strong>Testing:</strong> <%= row.testingRequirements %>
                </div>
              <% } %>
              <% if (row.totalPrice) { %>
                <div class="text-muted" style="font-size: 8px; margin-top: 4px;">Total: <%= row.totalPrice %></div>
              <% } %>
              <% if (row.updatedAt) { %>
                <div class="text-muted" style="font-size: 7px; margin-top: 4px;">Updated: <%= row.updatedAt %></div>
              <% } %>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>

    <div class="summary-grid">
      <div class="summary-card">
        <h4>MATERIALS OVERVIEW</h4>
        <p>Total materials: <strong><%= bom.rows.length %></strong></p>
        <p>Categories: <strong><%= bom.stats.categories %></strong></p>
        <p>With images: <strong><%= bom.stats.withImages %></strong></p>
      </div>
      <div class="summary-card">
        <h4>SUPPLIER OVERVIEW</h4>
        <p>Unique suppliers: <strong><%= bom.stats.uniqueSuppliers %></strong></p>
        <p>Lead time avg: <strong><%= bom.stats.avgLeadTime %></strong></p>
        <p>MOQ avg: <strong><%= bom.stats.avgMoq %></strong></p>
      </div>
    </div>
  <% } else { %>
    <div style="padding:32px; text-align:center; color:#94a3b8;"><%= strings.emptyBom %></div>
  <% } %>
</body>
</html>

