<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>BOM Table</title>
  <style>
    @page {
      size: A4 landscape;
      margin: 20mm 25mm;
    }

    body {
      font-family: 'Roboto', 'Arial', 'Helvetica', 'DejaVu Sans', sans-serif;
      font-size: 11pt;
      line-height: 1.5;
      color: #1f2933;
      -webkit-print-color-adjust: exact;
      print-color-adjust: exact;
      margin: 0;
      padding: 0;
    }

    .section-title {
      font-size: 18pt;
      text-transform: uppercase;
      letter-spacing: 1px;
      border-bottom: 3px solid #111827;
      padding-bottom: 10px;
      margin: 0 0 24px 0;
      page-break-after: avoid;
      page-break-inside: avoid;
      break-after: avoid;
      break-inside: avoid;
      font-weight: 700;
      color: #0f172a;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 24px;
      background: #ffffff;
      font-size: 10pt;
      page-break-inside: avoid;
      break-inside: avoid;
    }

    tbody {
      page-break-inside: avoid;
      break-inside: avoid;
    }

    tr {
      page-break-inside: avoid;
      break-inside: avoid;
    }

    thead {
      display: table-header-group;
    }

    th, td {
      border: 1px solid #E0E0E0;
      padding: 10px 12px;
      vertical-align: top;
      word-break: break-word;
    }

    th {
      background: #E0E0E0;
      text-transform: uppercase;
      font-size: 12pt;
      letter-spacing: 0.4px;
      color: #0f172a;
      font-weight: 700;
    }

    tr:nth-child(even) td {
      background: #f9fafb;
    }

    .text-right { text-align: right; }
    .text-center { text-align: center; }
    .text-muted { color: #6b7280; }
    .avoid-break { page-break-inside: avoid; break-inside: avoid; }

    .color-swatch {
      display: inline-block;
      width: 16px;
      height: 16px;
      border: 1px solid #cbd5e1;
      border-radius: 3px;
      vertical-align: middle;
      flex-shrink: 0;
    }

    .thumb {
      width: 60px;
      height: 60px;
      object-fit: cover;
      border: 1px solid #d1d5db;
      border-radius: 4px;
      background: #fff;
      display: block;
    }

    .summary-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 18px;
      margin-top: 24px;
    }

    .summary-card {
      border: 1px solid #E0E0E0;
      border-radius: 6px;
      padding: 12px;
      background: #f8fafc;
      page-break-inside: avoid;
    }

    .summary-card h4 {
      font-size: 14pt;
      text-transform: uppercase;
      margin-bottom: 10px;
      font-weight: 700;
      color: #0f172a;
    }

    .summary-card p {
      margin-bottom: 6px;
      font-size: 11pt;
    }
  </style>
</head>
<body>
  <%- include('article-summary', { meta, images }) %>
  <div class="section-title"><%= strings.bomTitle %></div>

  <% if (bom && bom.rows && bom.rows.length) { %>
    <table class="bom-table">
    <thead>
      <tr>
        <th style="width:3%;">#</th>
        <th style="width:8%;">TYPE</th>
        <th style="width:12%;">ITEM</th>
        <th style="width:10%;">PART</th>
        <th style="width:9%;">PLACEMENT</th>
        <th style="width:5%;" class="text-center">QTY</th>
        <th style="width:5%;" class="text-center">UOM</th>
        <th style="width:12%;">SUPPLIER</th>
        <th style="width:36%;">NOTES</th>
      </tr>
    </thead>
      <tbody>
        <% bom.rows.forEach((row, idx) => { %>
          <tr class="avoid-break">
            <td class="text-center"><%= row.lineNumber || idx + 1 %></td>
            <td class="text-center">
              <span style="font-weight:600; font-size:9pt; color:#475569;"><%= row.materialType || row.category || '—' %></span>
            </td>
            <td>
              <div><strong><%= row.materialName %></strong></div>
              <% if (row.materialCode) { %>
                <div class="text-muted" style="font-size: 9pt;">Code: <%= row.materialCode %></div>
              <% } %>
            </td>
            <td>
              <div><%= row.part || '—' %></div>
              <% if (row.size) { %>
                <div class="text-muted" style="font-size: 9pt;">Size: <%= row.size %></div>
              <% } %>
            </td>
            <td><%= row.placement || '—' %></td>
            <td class="text-center"><%= row.quantity || '—' %></td>
            <td class="text-center"><%= row.uom || '—' %></td>
            <td>
              <div><%= row.supplier || '—' %></div>
              <% if (row.supplierCode) { %>
                <div class="text-muted" style="font-size: 9pt;">Code: <%= row.supplierCode %></div>
              <% } %>
            </td>
            <td>
              <div><%= row.comments || '—' %></div>
              <% if (row.hexCode || row.pantone) { %>
                <div style="margin-top:6px; font-size:9pt;">
                  <% if (row.hexCode) { %>
                    <span class="color-swatch" style="background-color: <%= row.hexCode %>; width: 14px; height: 14px; border: 1px solid #E0E0E0; border-radius: 3px; display: inline-block; vertical-align: middle; margin-right: 6px;"></span>
                    <%= row.hexCode %>
                  <% } %>
                  <% if (row.pantone) { %>
                    <% if (row.hexCode) { %> / <% } %>Pantone: <%= row.pantone %>
                  <% } %>
                </div>
              <% } %>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>

    <div class="summary-grid">
      <div class="summary-card">
        <h4>MATERIALS OVERVIEW</h4>
        <p>Total materials: <strong><%= bom.rows.length %></strong></p>
        <p>Categories: <strong><%= bom.stats.categories %></strong></p>
        <p>With images: <strong><%= bom.stats.withImages %></strong></p>
      </div>
      <div class="summary-card">
        <h4>SUPPLIER OVERVIEW</h4>
        <p>Unique suppliers: <strong><%= bom.stats.uniqueSuppliers %></strong></p>
        <p>Lead time avg: <strong><%= bom.stats.avgLeadTime %></strong></p>
        <p>MOQ avg: <strong><%= bom.stats.avgMoq %></strong></p>
      </div>
    </div>
  <% } else { %>
    <div style="padding:32px; text-align:center; color:#94a3b8; font-size:11pt;"><%= strings.emptyBom %></div>
  <% } %>
</body>
</html>

