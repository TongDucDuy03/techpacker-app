<style>
  .pdf-header {
    width: 100%;
    font-family: 'Roboto', Arial, sans-serif;
    font-size: 9px;
    color: #475569;
    padding: 6px 0;
    border-bottom: 1px solid #cbd5e1;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
  .pdf-header__title {
    font-weight: 600;
    color: #0f172a;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }
  .pdf-header__meta {
    text-align: right;
  }
</style>
<div class="pdf-header">
  <div>
    <div class="pdf-header__title"><%= meta.articleCode %> — <%= meta.productName %></div>
    <div>Season: <%= meta.season || 'N/A' %> · Version: <%= meta.version %></div>
  </div>
  <div class="pdf-header__meta">
    <div>Printed by: <%= printedBy %></div>
    <div>Date: <%= generatedAt %></div>
  </div>
</div>
