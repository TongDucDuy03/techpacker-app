<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>TechPack Cover</title>
  <style>
    @page {
      size: A4;
      margin: 15mm 20mm;
    }

    body {
      font-family: 'Roboto', 'Arial', 'Helvetica', 'DejaVu Sans', sans-serif;
      font-size: 10px;
      line-height: 1.45;
      color: #1f2933;
      -webkit-print-color-adjust: exact;
      print-color-adjust: exact;
      margin: 0;
      padding: 0;
    }
    
    @font-face {
      font-family: 'Roboto';
      font-style: normal;
      font-weight: 400;
      src: local('Roboto'), local('Roboto-Regular');
    }

    .cover-header {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      border-bottom: 2px solid #1f2933;
      padding-bottom: 18px;
      margin-bottom: 20px;
    }

    .cover-title {
      font-size: 20px;
      text-transform: uppercase;
      letter-spacing: 1px;
      margin-bottom: 6px;
      font-weight: 600;
    }

    .cover-meta {
      font-size: 11px;
      color: #4b5563;
      margin-bottom: 4px;
    }

    .cover-info {
      font-size: 10px;
      color: #6b7280;
    }

    .cover-image-container {
      text-align: center;
    }

    .cover-image {
      width: 100%;
      height: 280px;
      object-fit: cover;
      border: 1px solid #cbd5e1;
      border-radius: 6px;
      image-rendering: -webkit-optimize-contrast;
      image-rendering: crisp-edges;
    }

    .no-image {
      width: 100%;
      height: 220px;
      border: 1px dashed #cbd5e1;
      border-radius: 6px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #9ca3af;
      font-size: 10px;
      text-transform: uppercase;
    }

    .meta-table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 18px;
    }

    .meta-table th {
      width: 28%;
      text-align: left;
      padding: 8px 6px;
      border: 1px solid #cbd5e1;
      background: #f1f5f9;
      font-size: 9px;
      font-weight: 600;
    }

    .meta-table td {
      padding: 8px 6px;
      border: 1px solid #cbd5e1;
    }

    .summary-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 12px;
      margin-top: 16px;
    }

    .summary-card {
      border: 1px solid #d1d5db;
      border-radius: 6px;
      padding: 12px;
      background: #f8fafc;
    }

    .summary-card h4 {
      font-size: 10px;
      text-transform: uppercase;
      margin-bottom: 6px;
      font-weight: 600;
    }

    .summary-card p {
      margin-bottom: 4px;
      font-size: 9px;
    }

    .text-muted {
      color: #6b7280;
    }
  </style>
</head>
<body>
  <div class="cover-header">
    <div>
      <h1 class="cover-title"><%= meta.productName %></h1>
      <p class="cover-meta">
        <strong>Article:</strong> <%= meta.articleCode %> &nbsp;|&nbsp;
        <strong>Version:</strong> <%= meta.version %>
      </p>
      <p class="cover-info">
        <strong>Season:</strong> <%= meta.season || 'N/A' %> &nbsp;|&nbsp;
        <strong>Lifecycle:</strong> <%= meta.lifecycleStage || 'N/A' %>
      </p>
    </div>
    <div style="text-align:right; font-size:9px;">
      <!-- Logo placeholder - có thể thay bằng logo thật từ config -->
      <div style="margin-bottom:8px; padding:8px; border:1px solid #cbd5e1; border-radius:4px; display:inline-block; background:#fff;">
        <div style="font-size:10px; font-weight:600; color:#1f2933;">TECHPACKER</div>
      </div>
      <p class="text-muted">Printed by: <%= printedBy %></p>
      <p class="text-muted">Generated at: <%= generatedAt %></p>
    </div>
  </div>

  <div style="display:flex; gap:18px;">
    <div style="flex:2;">
      <table class="meta-table">
        <tbody>
          <tr>
            <th>Designer</th>
            <td><%= meta.designer || '—' %></td>
          </tr>
          <tr>
            <th>Supplier</th>
            <td><%= meta.supplier || '—' %></td>
          </tr>
          <tr>
            <th>Category</th>
            <td><%= meta.category || '—' %></td>
          </tr>
          <tr>
            <th>Gender</th>
            <td><%= meta.gender || '—' %></td>
          </tr>
          <tr>
            <th>Brand</th>
            <td><%= meta.brand || '—' %></td>
          </tr>
          <tr>
            <th>Collection</th>
            <td><%= meta.collectionName || '—' %></td>
          </tr>
          <tr>
            <th>Fabric</th>
            <td><%= meta.fabricDescription || '—' %></td>
          </tr>
          <tr>
            <th>Created / Updated</th>
            <td>
              <%= meta.createdAtFormatted || meta.createdAt || '—' %>
              &nbsp;→&nbsp;
              <%= meta.updatedAtFormatted || meta.updatedAt || '—' %>
            </td>
          </tr>
          <% if (meta.retailPrice) { %>
            <tr>
              <th>Retail Price</th>
              <td><%= meta.retailPrice %> <%= meta.currency || '' %></td>
            </tr>
          <% } %>
          <% if (meta.description) { %>
            <tr>
              <th>Description</th>
              <td><%= meta.description %></td>
            </tr>
          <% } %>
        </tbody>
      </table>
    </div>
    <div style="flex:1;" class="cover-image-container">
      <% if (images && images.coverImage) { %>
        <img src="<%= images.coverImage %>" alt="Product Design Sketch" class="cover-image" style="max-width: 100%; height: auto;" />
      <% } else { %>
        <div class="no-image">No product image available</div>
      <% } %>
    </div>
  </div>

  <div class="summary-grid">
    <div class="summary-card">
      <h4>Bill of Materials</h4>
      <p>Total Items: <strong><%= summary.bomCount %></strong></p>
      <p>Suppliers: <strong><%= summary.uniqueSuppliers %></strong></p>
      <p>Approved: <strong><%= summary.approvedMaterials %></strong></p>
    </div>

    <div class="summary-card">
      <h4>Measurements</h4>
      <p>Total POM: <strong><%= summary.measurementCount %></strong></p>
      <p>Critical Points: <strong><%= summary.criticalMeasurements %></strong></p>
      <p>Size Range: <strong><%= summary.sizeRange || '—' %></strong></p>
    </div>

    <div class="summary-card">
      <h4>How to Measure</h4>
      <p>Steps: <strong><%= summary.howToMeasureCount %></strong></p>
      <p>Diagrams: <strong><%= summary.howToMeasureWithImage %></strong></p>
      <p>Tips: <strong><%= summary.howToMeasureTips %></strong></p>
    </div>

    <div class="summary-card">
      <h4>Notes & Care</h4>
      <p>Notes: <strong><%= summary.notesCount %></strong></p>
      <p>Care Symbols: <strong><%= summary.careSymbolCount %></strong></p>
      <p>Last Export: <strong><%= summary.lastExport || '—' %></strong></p>
    </div>

    <% if (summary.colorwaysCount > 0 || summary.sampleRoundsCount > 0) { %>
    <div class="summary-card">
      <h4>Additional Info</h4>
      <% if (summary.colorwaysCount > 0) { %>
        <p>Colorways: <strong><%= summary.colorwaysCount %></strong></p>
      <% } %>
      <% if (summary.sampleRoundsCount > 0) { %>
        <p>Sample Rounds: <strong><%= summary.sampleRoundsCount %></strong></p>
      <% } %>
    </div>
    <% } %>
  </div>
</body>
</html>


