<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>TechPack Cover</title>
  <style>
    @page {
      size: A4 landscape;
      margin: 20mm 25mm;
    }

    body {
      font-family: 'Roboto', 'Arial', 'Helvetica', 'DejaVu Sans', sans-serif;
      font-size: 11pt;
      line-height: 1.5;
      color: #1f2933;
      -webkit-print-color-adjust: exact;
      print-color-adjust: exact;
      margin: 0;
      padding: 0;
    }
    
    @font-face {
      font-family: 'Roboto';
      font-style: normal;
      font-weight: 400;
      src: local('Roboto'), local('Roboto-Regular');
    }

    .cover-header {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      border-bottom: 2px solid #1f2933;
      padding-bottom: 16px;
      margin-bottom: 24px;
    }

    .cover-title {
      font-size: 20pt;
      text-transform: uppercase;
      letter-spacing: 0.8px;
      margin-bottom: 8px;
      font-weight: 700;
      color: #0f172a;
    }

    .cover-meta {
      font-size: 12pt;
      color: #475569;
      margin-bottom: 4px;
      font-weight: 600;
    }

    .cover-info {
      font-size: 10pt;
      color: #64748b;
    }

    .cover-date {
      font-size: 10pt;
      color: #94a3b8;
      text-align: right;
      margin-top: 4px;
    }

    .cover-image-container {
      text-align: center;
    }

    .cover-image {
      width: 100%;
      max-height: 200px;
      object-fit: contain;
      border: 1px solid #E0E0E0;
      border-radius: 4px;
      image-rendering: -webkit-optimize-contrast;
      image-rendering: crisp-edges;
    }

    .no-image {
      width: 100%;
      height: 180px;
      border: 1px solid #E0E0E0;
      border-radius: 6px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #64748b;
      font-size: 11pt;
      background: #f8fafc;
    }

    .fabric-notes-box {
      margin-top: 18px;
      padding: 12px;
      border: 1px solid #E0E0E0;
      border-radius: 6px;
      background: #f8fafc;
      page-break-inside: avoid;
    }

    .fabric-notes-label {
      font-size: 12pt;
      text-transform: uppercase;
      color: #0f172a;
      margin-bottom: 8px;
      font-weight: 700;
    }

    .fabric-notes-content {
      font-size: 11pt;
      color: #1f2933;
      line-height: 1.5;
    }

    .cover-layout {
      display: grid;
      grid-template-columns: 2fr 1fr;
      gap: 24px;
      margin-bottom: 24px;
    }

    .cover-info-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 16px;
    }

    .info-group {
      margin-bottom: 16px;
    }

    .info-group-label {
      font-size: 10pt;
      text-transform: uppercase;
      color: #64748b;
      margin-bottom: 6px;
      font-weight: 600;
      letter-spacing: 0.3px;
    }

    .info-group-value {
      font-size: 11pt;
      color: #0f172a;
      font-weight: 500;
    }

    .meta-table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 16px;
    }

    .meta-table th {
      width: 30%;
      text-align: left;
      padding: 10px 12px;
      border: 1px solid #E0E0E0;
      background: #E0E0E0;
      font-size: 12pt;
      font-weight: 700;
      text-transform: uppercase;
    }

    .meta-table td {
      padding: 10px 12px;
      border: 1px solid #E0E0E0;
      font-size: 11pt;
    }

    .summary-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 18px;
      margin-top: 24px;
    }

    .summary-card {
      border: 1px solid #E0E0E0;
      border-radius: 6px;
      padding: 12px;
      background: #f8fafc;
      page-break-inside: avoid;
    }

    .summary-card h4 {
      font-size: 14pt;
      text-transform: uppercase;
      margin-bottom: 10px;
      font-weight: 700;
      color: #0f172a;
    }

    .summary-card p {
      margin-bottom: 6px;
      font-size: 11pt;
    }

    .text-muted {
      color: #6b7280;
    }
  </style>
</head>
<body>
  <%- include('article-summary', { meta, images }) %>
  <div class="cover-header">
    <div>
      <% if (images && images.companyLogo) { %>
        <div style="margin-bottom:12px;">
          <img src="<%= images.companyLogo %>" alt="Company Logo" style="max-height:50px; max-width:200px; object-fit:contain;" />
        </div>
      <% } %>
      <h1 class="cover-title"><%= meta.productName %></h1>
      <div class="cover-meta">
        <strong>Style / Article:</strong> <%= meta.articleCode %> &nbsp;|&nbsp;
        <strong>Version:</strong> <%= meta.version %> &nbsp;|&nbsp;
        <strong>Season:</strong> <%= meta.season || 'N/A' %>
      </div>
    </div>
    <div class="cover-date">
      <%= generatedAt %>
    </div>
  </div>

  <div class="cover-layout">
    <div>
      <div class="cover-info-grid">
        <div class="info-group">
          <div class="info-group-label">Brand</div>
          <div class="info-group-value"><%= meta.brand || '—' %></div>
        </div>
        <div class="info-group">
          <div class="info-group-label">Category</div>
          <div class="info-group-value"><%= meta.category || '—' %></div>
        </div>
        <div class="info-group">
          <div class="info-group-label">Designer</div>
          <div class="info-group-value"><%= meta.designer || '—' %></div>
        </div>
        <div class="info-group">
          <div class="info-group-label">Supplier</div>
          <div class="info-group-value"><%= meta.supplier || '—' %></div>
        </div>
        <div class="info-group">
          <div class="info-group-label">Gender</div>
          <div class="info-group-value"><%= meta.gender || '—' %></div>
        </div>
        <div class="info-group">
          <div class="info-group-label">Collection</div>
          <div class="info-group-value"><%= meta.collectionName || '—' %></div>
        </div>
      </div>

      <% if (meta.fabricDescription || meta.description || meta.notes) { %>
        <div class="fabric-notes-box">
          <div class="fabric-notes-label">Fabric & Notes</div>
          <div class="fabric-notes-content">
            <% if (meta.fabricDescription) { %>
              <div><strong>Fabric:</strong> <%= meta.fabricDescription %></div>
            <% } %>
            <% if (meta.description) { %>
              <div style="margin-top:6px;"><strong>Description:</strong> <%= meta.description %></div>
            <% } %>
            <% if (meta.notes) { %>
              <div style="margin-top:6px;"><strong>Notes:</strong> <%= meta.notes %></div>
            <% } %>
          </div>
        </div>
      <% } %>
    </div>
    <div class="cover-image-container">
      <% if (images && images.coverImage) { %>
        <img src="<%= images.coverImage %>" alt="Product Design Sketch" class="cover-image" />
      <% } else { %>
        <div class="no-image">No product image available</div>
      <% } %>
    </div>
  </div>

  <div class="summary-grid">
    <div class="summary-card">
      <h4>Bill of Materials</h4>
      <p>Total Items: <strong><%= summary.bomCount %></strong></p>
      <p>Suppliers: <strong><%= summary.uniqueSuppliers %></strong></p>
      <p>Approved: <strong><%= summary.approvedMaterials %></strong></p>
    </div>

    <div class="summary-card">
      <h4>Measurements</h4>
      <p>Total POM: <strong><%= summary.measurementCount %></strong></p>
      <p>Critical Points: <strong><%= summary.criticalMeasurements %></strong></p>
      <p>Size Range: <strong><%= summary.sizeRange || '—' %></strong></p>
    </div>

    <div class="summary-card">
      <h4>How to Measure</h4>
      <p>Steps: <strong><%= summary.howToMeasureCount %></strong></p>
      <p>Diagrams: <strong><%= summary.howToMeasureWithImage %></strong></p>
      <p>Tips: <strong><%= summary.howToMeasureTips %></strong></p>
    </div>

    <div class="summary-card">
      <h4>Notes & Care</h4>
      <p>Notes: <strong><%= summary.notesCount %></strong></p>
      <p>Care Symbols: <strong><%= summary.careSymbolCount %></strong></p>
      <p>Last Export: <strong><%= summary.lastExport || '—' %></strong></p>
    </div>

    <% if (summary.colorwaysCount > 0 || summary.sampleRoundsCount > 0) { %>
    <div class="summary-card">
      <h4>Additional Info</h4>
      <% if (summary.colorwaysCount > 0) { %>
        <p>Colorways: <strong><%= summary.colorwaysCount %></strong></p>
      <% } %>
      <% if (summary.sampleRoundsCount > 0) { %>
        <p>Sample Rounds: <strong><%= summary.sampleRoundsCount %></strong></p>
      <% } %>
    </div>
    <% } %>
  </div>
</body>
</html>


